<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        `appCategory` is not cosmetic here.

        From Android 16 (which this app targets), orientation, aspect-ratio and
        resizability restrictions are ignored on any display at least 600dp
        wide - so `screenOrientation="portrait"` below stops working on tablets
        and unfolded foldables, and a three-lane runner laid out for a portrait
        phone ends up stretched across a landscape one. Games are exempt from
        that change, and the flag is how the platform knows this is a game.

        The general escape hatch is the PROPERTY_COMPAT_ALLOW_RESTRICTED_-
        RESIZABILITY manifest property, which is the wrong tool: it is removed
        in API 37, whereas the exemption is not going anywhere.
    -->
    <application
        android:allowBackup="true"
        android:appCategory="game"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Haptic feedback on lane changes, pickups and crashes. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- The game is a real-time 3D renderer; without this it can be installed
         on devices that cannot hold a playable frame rate. -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
</manifest>
